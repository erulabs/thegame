'use strict';

console.log('GAME TESTS STARTING:');

global.expect = require('chai').expect;

// let User = require('./../build/api/models/User.js');

describe('GAME', function () {
  it('it should have a test', function () {
    expect(true).to.be.a('boolean');
  });
});
