'use strict';

const SharedUser = require('./../../shared/models/User.js');

/**
 * Represents a User - supports authentication
 * This is a test of the auto-doc system
 * @constructor
 */
class User extends SharedUser {
  constructor() {
  }
}

module.exports = User;
